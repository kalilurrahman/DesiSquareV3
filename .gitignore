# Dependencies
node_modules/

# Service runtime state (JSON-file persistence; recreated on boot).
# Ignore the runtime files only — NOT committed seed content (e.g. v4/data/seed.json).
**/state.json
**/data/db.json
**/data/db.json.tmp-*

# Local secrets — never commit; samples are .env.example
.env
**/.env
!**/.env.example

# OS / editor
.DS_Store
*.log
